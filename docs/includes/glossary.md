*[O(1)]: Constant time — cost doesn't grow with input size
*[O(n)]: Linear time — cost grows proportionally with input size
*[O(n²)]: Quadratic time — cost grows with the square of input size
*[O(n log n)]: Linearithmic time — typical cost of efficient sorting
*[SARIF]: Static Analysis Results Interchange Format — a standard JSON format for static analysis output
*[IR]: Intermediate Representation — Algorilla's language-agnostic code model
*[AST]: Abstract Syntax Tree — a tree representation of source code structure
*[JPA]: Java Persistence API — the standard Java ORM specification
*[ORM]: Object-Relational Mapping — framework that maps database tables to objects
*[GORM]: Grails Object Relational Mapping — Groovy/Grails ORM framework
*[GDK]: Groovy Development Kit — Groovy's extension methods on standard Java types
*[SFC]: Single-File Component — Vue.js component format combining template, script, and style
*[HOF]: Higher-Order Function — a function that takes or returns other functions
*[NFA]: Nondeterministic Finite Automaton — used internally by regex engines
*[DFA]: Deterministic Finite Automaton — optimized form of regex matching
*[JDBC]: Java Database Connectivity — Java's standard database access API
*[CI]: Continuous Integration — automated build and test pipeline
*[CD]: Continuous Deployment — automated release pipeline
