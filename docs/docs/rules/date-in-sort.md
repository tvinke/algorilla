# Date in Sort

This rule has been merged into [Expensive Sort Comparator](expensive-sort-comparator.md).

The `date-in-sort` ID is still accepted in suppress comments and configuration for backwards compatibility.
